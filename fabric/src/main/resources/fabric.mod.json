{
  "schemaVersion": 1,
  "id": "tobyscamera",
  "version": "${version}",
  "name": "TobysCamera",
  "license": "MIT",
  "environment": "client",
  "entrypoints": { "client": ["dev.tobyscamera.fabric.TobysCameraClient"] },
  "mixins": ["tobyscamera.mixins.json"],
  "depends": { "fabricloader": ">=${fabric_loader_version}", "minecraft": "${minecraft_version_range}", "java": ">=${java_version}", "fabric-api": "*" }
}
